<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Perguntas e Respostas - Rocket-Q</title>
    <!-- Importação de estilos CSS -->
    <link rel="stylesheet" href="/styles/global.css" />
    <link rel="stylesheet" href="/styles/room.css" />
    <link rel="stylesheet" href="/styles/modal.css" />

    <!-- scripts js -->
    <script src="/scripts/main.js" defer type="module" ></script>


    <!-- Fonts Google -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@0,300..800;1,300..800&display=swap"
        rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
        rel="stylesheet">
</head>

<body>
    <div id="room" class="content">
        <header>
            <a href="/">
                <img src="/images/logo.svg" alt="Rocket-Q logo" />
            </a>

            <div class="buttons">
                <div class="button button-create-room" id="room-id" data-id="<%= roomId %>">
                    <%= roomId %>
                    <img src="/images/copy.svg" alt="Icone de copiar conteudo" />
                </div>
                <a href="/create-pass" class="button">
                    <img src="/images/users-pass.svg" alt="Icone de users" />
                    Criar nova sala
                </a>
            </div>
        </header>
        <main>
            <div class="container">
                <section id="question-form">
                    <h2>Faça a sua pergunta</h2>
                    <form action="/create-question" method="post">
                        <label for="question" class="sr-only">O que você quer perguntar</label>
                        <input type="hidden" name="room" id="room" value="<%= roomId %>">
                        <textarea name="question" id="question" placeholder="O que você quer perguntar?" rows="3" required maxlength="255"></textarea>
    
                        <footer>
                            <div>
                                <img src="/images/lock.svg" alt="Icone de cadeado" />
                                Está pergunta é anônima
                            </div>
                            <button type="submit" class="button">Enviar</button>
                        </footer>
                    </form>
                </section>

                <section id="questions-section">
                    <h2 class="sr-only">Perguntas da comunidade</h2>

                    <% if(isNotQuestion) { %>
                        <%- include(`partials/notquestions`) %>
                    <% } %>
                   
                    <% questions.forEach(question => { %>

                    <div class="question-wrapper">
                        <div class="question-content">
                            <div class="avatar">
                                <img src="/images/user-avatar.svg" alt="Icone de avatar">
                            </div>
                            <p>
                                <%= question.title %>
                            </p>
                        </div>
                        <div class="actions">
                            <a href="#" class="check" data-id="<%= question.id %>">
                                <img src="/images/check.svg" alt="Icone de check">
                                Marcar como lida
                            </a>
                            <a href="#" class="delete" data-id="<%= question.id %>">
                                <img src="/images/trash.svg" alt="Icone de deletar">
                                Excluir
                            </a>
                        </div>
                    </div>

                     <% }) %>

                    <!-- Perguntas lidas -->
                    
                    <% questionsRead.forEach(question => { %>
                     <div class="question-wrapper read">
                        <div class="question-content">
                            <div class="avatar read-avatar">
                                <img src="/images/user-avatar.svg" alt="Icone de avatar">
                            </div>
                            <p>
                                <%= question.title %>
                            </p>
                        </div>
                        <div class="actions">
                            <a href="#" class="delete" data-id="<%= question.id %>">
                                <img src="/images/trash.svg" alt="Icone de deletar">
                                Excluir
                            </a>
                        </div>
                    </div> 
                     <% }) %>

                </section>
            </div>
        </main>
    </div>
    <!-- Modal  -->
        <div class="modal-wrapper">
            <div class="modal">
                <h3>Excluir pergunta</h3>
                <p>Tem certeza que você deseja excluir essa pergunta?</p>
                <form action="#" method="post"> 
                    <label for="password" class="sr-only">Digite sua senha</label>
                    <input type="password" id="password" name="password" placeholder="Digite sua senha">
                    <div class="buttons">
                        <a href="#" class="gray cancel">Cancelar</a>
                        <button class="red">Sim, excluir</button>
                    </div>
                </form>
            </div>
        </div>
</body>

</html>